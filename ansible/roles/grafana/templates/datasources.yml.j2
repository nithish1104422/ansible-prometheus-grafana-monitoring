apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ groups['prometheus_servers'][0] }}:9090
    isDefault: true
    editable: true
    basicAuth: false
